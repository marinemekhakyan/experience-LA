import React, { Component } from 'react';
import Nav from '../nav';
import Video from '../video';

class Home extends Component {
    render() {
        return (
            <div>
            < Nav />
            < Video />
            </div>
        )
    }
}

export default Home;